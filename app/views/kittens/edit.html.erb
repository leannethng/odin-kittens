<h1> Edit Kitten </h1>

<%= render 'form' %>

<%= link_to 'Delete', kitten_path(@kitten), 
              method: :delete,
              data: { confirm: 'Are you sure?'} %>

<%= link_to 'Back', kittens_path %>